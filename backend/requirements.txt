# TourVisor Bot Dependencies
# Responses API Migration
# Версии зафиксированы для воспроизводимости сборки

# HTTP клиент для TourVisor API
httpx>=0.27.0,<1.0.0

# Загрузка переменных окружения
python-dotenv>=1.0.0,<2.0.0

# OpenAI SDK (для GPT-5 Mini и совместимых моделей)
openai>=1.50.0,<2.0.0

# HTTP клиент (для Yandex Completion API fallback)
requests>=2.31.0,<3.0.0

# Web UI
flask>=3.0.0,<4.0.0
flask-cors>=4.0.0,<5.0.0
